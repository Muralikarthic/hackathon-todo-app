name: hackathon_todo_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  # firebase_core: ^3.1.1
  # firebase_auth: ^5.1.1
  # google_sign_in: ^6.2.1
  # firebase_crashlytics: ^4.0.2

  # State Management (Riverpod)
  flutter_riverpod: ^2.5.1

  # Local Database
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.0.15

  # Utility & UI
  intl: ^0.19.0
  uuid: ^4.4.0
  flutter_slidable: ^3.1.0
  lottie: ^3.1.2 # For animations
  confetti: ^0.7.0
  flutter_local_notifications: ^19.3.1
  timezone: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

  # Code Generation
  build_runner: ^2.4.11
  hive_generator: ^2.0.1
  riverpod_generator: ^2.4.2 # For Riverpod code generation
  riverpod_lint: ^2.3.10

flutter:
  uses-material-design: true
  assets:
    - assets/google_logo.png 
    - assets/no_tasks.json